


thetas = linspace(0, 2*pi, 50);
x = zeros(1,length(thetas));
y = zeros(1, length(thetas));

for itor = 1 : length(thetas)
    x(itor) = 32.5 * cos(thetas(itor));
    y(itor) = 32.5 * sin(thetas(itor));
end

contact_points = [x' y' zeros(1,length(x))'];